name: Messaging Service CI

on:
  push:
    paths:
      - 'services/messaging-service/**'
      - 'common/**'
  pull_request:
    paths:
      - 'services/messaging-service/**'
      - 'common/**'

jobs:
  build:
    name: Build & Test Messaging Service
    runs-on: ubuntu-latest
    
    services:
      postgres:
        image: postgres:17
        env:
          POSTGRES_USER: sportlink
          POSTGRES_PASSWORD: sportlink
          POSTGRES_DB: messages_test
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: gradle

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Build Messaging Service
        run: ./gradlew :services:messaging-service:build -x test --no-daemon

      - name: Run Messaging Service tests
        run: ./gradlew :services:messaging-service:test --no-daemon

      - name: Upload test results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: messaging-service-test-results
          path: 'services/messaging-service/build/test-results/'

      - name: Build Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: services/messaging-service/Dockerfile
          push: false
          tags: messaging-service:${{ github.sha }}
