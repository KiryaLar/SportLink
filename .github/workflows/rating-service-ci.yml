name: Rating Service CI

on:
  push:
    paths:
      - 'services/rating-service/**'
      - 'build-logic/**'
      - 'common/**'
  pull_request:
    paths:
      - 'services/rating-service/**'
      - 'build-logic/**'
      - 'common/**'

jobs:
  ci:
    uses: ./.github/workflows/_service-ci.yml
    with:
      service-name: rating-service
      needs-postgres: true
      needs-kafka: false
      test-db-name: ratings_test
