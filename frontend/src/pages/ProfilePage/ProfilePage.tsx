import React from 'react';
import { Container, Typography, Box } from '@mui/material';

export const ProfilePage: React.FC = () => {
  return (
    <Container>
      <Box sx={{ py: 4 }}>
        <Typography variant="h4">Profile Page - В разработке</Typography>
      </Box>
    </Container>
  );
};

export default ProfilePage;
